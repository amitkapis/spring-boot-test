plugins {
  id "org.sonarqube" version "3.3"
}

sonarqube {
  properties {
    property "sonar.projectKey", "amitkapis_spring-boot-test"
    property "sonar.organization", "sonarqube-hello"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}
